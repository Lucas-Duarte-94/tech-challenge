spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:postgresql://tech_challenge_db:5432/tech_challenge
    username: fiap_tech_challenge
    password: fiap123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework: DEBUG